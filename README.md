# gpt

## run

```
Usage of /Users/dave/bin/gpt:
  -ele
    	lookup elevations (default true)
  -output string
    	output dir (default "./output")
  -points string
    	all points file (default "./input/All Points.kmz")
  -tracks string
    	all tracks file (default "./input/All Tracks.kmz")
```
